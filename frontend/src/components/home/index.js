import React from 'react'
import Template from '../template'
import MotionForm from './MotionForm'

const Home = (props) => {
  return (
    <Template>
      <MotionForm/>
    </Template>
  );
};

export default Home;
