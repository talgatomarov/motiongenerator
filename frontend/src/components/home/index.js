import React from 'react'
import Template from '../template'
import Hero from './hero'

const Home = (props) => {
  return (
    <Template>
      <Hero/>
    </Template>
  );
};

export default Home;
